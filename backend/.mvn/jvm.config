# JVM Options for Low Memory Systems
# Reduce heap size
-Xms256m
-Xmx512m

# Reduce metaspace
-XX:MetaspaceSize=128m
-XX:MaxMetaspaceSize=256m

# Use G1GC with smaller regions
-XX:+UseG1GC
-XX:G1HeapRegionSize=4m

# Reduce GC overhead
-XX:MaxGCPauseMillis=200
-XX:ParallelGCThreads=2
-XX:ConcGCThreads=1

# Reduce reserved code cache
-XX:ReservedCodeCacheSize=128m

# Disable tiered compilation for faster startup with less memory
-XX:-TieredCompilation
